openapi: 3.2.0
info:
  title: Adobe Suite Tfa/provider/duo Security/configure API
  version: 2.4.6
  description: 'Operations tagged tfa/provider/duo_security/configure across 2 of this provider''s published API definitions: adobe-suite-commerce-rest-customer-openapi.yaml, adobe-suite-commerce-rest-guest-openapi.yaml. Each path carries the servers of the definition it was published in.'
servers:
- url: https://example.com/rest/default
tags:
- name: tfa/provider/duo_security/configure
paths:
  /V1/tfa/provider/duo_security/configure:
    post:
      tags:
      - tfa/provider/duo_security/configure
      description: Get the information required to configure duo
      operationId: PostV1TfaProviderDuo_securityConfigure
      responses:
        '200':
          description: 200 Success.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/two-factor-auth-data-duo-data-interface'
            application/xml:
              schema:
                $ref: '#/components/schemas/two-factor-auth-data-duo-data-interface'
        default:
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/error-response'
            application/xml:
              schema:
                $ref: '#/components/schemas/error-response'
      summary: tfa/provider/duo_security/configure
      requestBody:
        content:
          application/json:
            schema:
              required:
              - tfaToken
              properties:
                tfaToken:
                  type: string
              type: object
              xml:
                name: request
          application/xml:
            schema:
              required:
              - tfaToken
              properties:
                tfaToken:
                  type: string
              type: object
              xml:
                name: request
    servers:
    - url: https://example.com/rest/default
components:
  schemas:
    error-errors:
      type: array
      description: Errors list
      items:
        $ref: '#/components/schemas/error-errors-item'
    error-errors-item:
      type: object
      description: Error details
      properties:
        message:
          type: string
          description: Error message
        parameters:
          $ref: '#/components/schemas/error-parameters'
    two-factor-auth-data-duo-data-extension-interface:
      type: object
      description: ExtensionInterface class for @see \Magento\TwoFactorAuth\Api\Data\DuoDataInterface
    error-parameters-item:
      type: object
      description: Error parameters item
      properties:
        resources:
          type: string
          description: ACL resource
        fieldName:
          type: string
          description: Missing or invalid field name
        fieldValue:
          type: string
          description: Incorrect field value
    error-response:
      type: object
      properties:
        message:
          type: string
          description: Error message
        errors:
          $ref: '#/components/schemas/error-errors'
        code:
          type: integer
          description: Error code
        parameters:
          $ref: '#/components/schemas/error-parameters'
        trace:
          type: string
          description: Stack trace
      required:
      - message
    two-factor-auth-data-duo-data-interface:
      type: object
      description: Represents the data needed to use duo
      properties:
        signature:
          type: string
          description: The signature
        api_hostname:
          type: string
          description: The api hostname
        extension_attributes:
          $ref: '#/components/schemas/two-factor-auth-data-duo-data-extension-interface'
      required:
      - signature
      - api_hostname
    error-parameters:
      type: array
      description: Error parameters list
      items:
        $ref: '#/components/schemas/error-parameters-item'
  securitySchemes:
    api_key:
      type: apiKey
      name: api_key
      in: header
x-refined-from:
- adobe-suite-commerce-rest-customer-openapi.yaml
- adobe-suite-commerce-rest-guest-openapi.yaml
x-original-swagger-version: '2.0'
